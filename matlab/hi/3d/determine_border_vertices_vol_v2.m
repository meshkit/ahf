function isborder = determine_border_vertices_vol_v2(nv, elems, opphfs) %#codegen
% Variance of determine_border_vertices_vol with 3 input arguments.

isborder = determine_border_vertices_vol(nv, elems, opphfs);
